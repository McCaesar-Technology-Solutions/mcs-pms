name: Scheduled crons

# Vercel Hobby only allows daily cron jobs in vercel.json. Sub-daily reservation
# jobs run here via CRON_SECRET instead.
#
# Required repo secrets: CRON_SECRET, PRODUCTION_APP_URL (e.g. https://mcs-pms.vercel.app)
#
# Note: job-level `if` cannot use the `env` context — use github.* only.

on:
  schedule:
    - cron: '*/5 * * * *'
    - cron: '0 * * * *'
    - cron: '15 * * * *'
    - cron: '30 * * * *'
    - cron: '0 6 * * *'
  workflow_dispatch:
    inputs:
      run_all:
        description: 'Run every cron job (not only the scheduled slice)'
        required: false
        default: 'true'
        type: choice
        options:
          - 'true'
          - 'false'

env:
  CRON_AUTH: 'Authorization: Bearer ${{ secrets.CRON_SECRET }}'

jobs:
  every-five-minutes:
    if: >-
      github.event.schedule == '*/5 * * * *' ||
      (github.event_name == 'workflow_dispatch' && github.event.inputs.run_all != 'false')
    runs-on: ubuntu-latest
    steps:
      - name: Drain notification outbox
        run: |
          curl -sfS \
            -H "$CRON_AUTH" \
            "${{ secrets.PRODUCTION_APP_URL }}/api/cron/notifications"
      - name: Expire reservation holds
        run: |
          curl -sfS \
            -H "$CRON_AUTH" \
            "${{ secrets.PRODUCTION_APP_URL }}/api/cron/reservation-holds"
      - name: Reclaim stuck access jobs
        run: |
          curl -sfS \
            -H "$CRON_AUTH" \
            "${{ secrets.PRODUCTION_APP_URL }}/api/cron/access-jobs"
      - name: Airbnb / iCal import sync
        run: |
          curl -sfS \
            -H "$CRON_AUTH" \
            "${{ secrets.PRODUCTION_APP_URL }}/api/cron/ical-sync"

  hourly-pre-arrival:
    if: >-
      github.event.schedule == '0 * * * *' ||
      (github.event_name == 'workflow_dispatch' && github.event.inputs.run_all != 'false')
    runs-on: ubuntu-latest
    steps:
      - name: Reservation pre-arrival reminders
        run: |
          curl -sfS \
            -H "$CRON_AUTH" \
            "${{ secrets.PRODUCTION_APP_URL }}/api/cron/reservation-pre-arrival"

  hourly-auto-checkout-prompt:
    if: >-
      github.event.schedule == '15 * * * *' ||
      (github.event_name == 'workflow_dispatch' && github.event.inputs.run_all != 'false')
    runs-on: ubuntu-latest
    steps:
      - name: Auto checkout prompt
        run: |
          curl -sfS \
            -H "$CRON_AUTH" \
            "${{ secrets.PRODUCTION_APP_URL }}/api/cron/reservation-auto-checkout-prompt"

  hourly-overstay:
    if: >-
      github.event.schedule == '30 * * * *' ||
      (github.event_name == 'workflow_dispatch' && github.event.inputs.run_all != 'false')
    runs-on: ubuntu-latest
    steps:
      - name: Reservation overstay check
        run: |
          curl -sfS \
            -H "$CRON_AUTH" \
            "${{ secrets.PRODUCTION_APP_URL }}/api/cron/reservation-overstay"

  daily-readiness:
    if: >-
      github.event.schedule == '0 6 * * *' ||
      (github.event_name == 'workflow_dispatch' && github.event.inputs.run_all != 'false')
    runs-on: ubuntu-latest
    steps:
      - name: Health check
        run: |
          curl -sfS "${{ secrets.PRODUCTION_APP_URL }}/api/health"
      - name: Readiness check
        run: |
          body=$(curl -sfS "${{ secrets.PRODUCTION_APP_URL }}/api/ready")
          echo "$body"
          echo "$body" | grep -q '"status":"ready"'
